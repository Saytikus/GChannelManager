# GChannelManager — документация

**GChannelManager** — Qt6/C++20 разделяемая библиотека для построения протокольного канала связи поверх произвольного транспорта (последовательный порт, UDP, RUDP и т.п.). Главный объект — `Gateway`: он управляет состоянием канала, сессией с keep-alive, отправкой/приёмом сообщений, повторами и статистикой.

> [!info] Где это пригодится
> Везде, где нужен надёжный обмен короткими кадрами по нестабильной линии: телеметрия, промышленные шины, радиоканалы, протоколы команд между микроконтроллером и хостом.

## Содержание

1. [[01-Обзор|Обзор]] — что это, зачем, ключевые возможности
2. [[02-Архитектура|Архитектура]] — слои и компоненты, диаграмма
3. [[03-Состояния-и-переходы|Состояния и переходы]] — `ChannelState`, `SessionState`
4. [[04-Протокол-и-кодек|Протокол и кодек]] — `IMessageCodec`, формат кадра
5. [[05-Транспорт|Транспорт]] — `ITransport` и его реализации
6. [[06-Gateway-API|Gateway API]] — публичный интерфейс класса
7. [[07-Статистика|Статистика]] — `GatewayStats`, `statsUpdated`
8. [[08-Сборка-и-интеграция|Сборка и интеграция]] — CMake, опции
9. [[09-Тестирование|Тестирование]] — Qt Test, `FakeTransport`
10. [[10-Руководство-пользователя|Руководство пользователя]] — пошаговые примеры

## Быстрая навигация по сущностям

| Сущность | Файл | Описание |
|---|---|---|
| `Gateway` | [[06-Gateway-API]] | Главный класс — управление каналом, сессией, отправкой |
| `GatewayRequest` | [[06-Gateway-API#GatewayRequest]] | Дескриптор запроса с ожиданием ответа |
| `GatewayStats` | [[07-Статистика]] | POD-снимок счётчиков |
| `ITransport` | [[05-Транспорт]] | Интерфейс транспорта |
| `IMessageCodec` | [[04-Протокол-и-кодек]] | Интерфейс кодека |
| `SimpleFrameCodec` | [[04-Протокол-и-кодек#SimpleFrameCodec]] | Эталонный кодек (пример) |
| `RetryPolicy` | [[06-Gateway-API#RetryPolicy]] | Политика повторов |
| `KeepAliveConfig` | [[06-Gateway-API#KeepAliveConfig]] | Настройки heartbeat |
